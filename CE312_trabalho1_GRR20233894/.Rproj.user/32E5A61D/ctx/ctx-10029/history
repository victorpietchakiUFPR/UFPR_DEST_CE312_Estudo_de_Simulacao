# Se o apply retornar transposto (o que depende do comportamento do R),
# forçamos a estrutura adequada:
if(ncol(S_t_estimado_ams_gg) == length(t_gg) && nrow(S_t_estimado_ams_gg) != length(t_gg)){
S_t_estimado_ams_gg <- t(S_t_estimado_ams_gg)
}
# Métrica: Média do Erro Relativo ao longo da curva (Independente da escala do tempo)
imre_por_simulacao <- apply(S_t_estimado_ams_gg, 2, function(S_estimada) {
mean((S_estimada - S_t_teorica_gg) / S_t_teorica_gg, na.rm = TRUE)
})
bias_global <- mean(imre_por_simulacao, na.rm = TRUE)
cens_perc <- mean(params_ams_gg[, "c"], na.rm = TRUE)
return(c(c = cens_perc, bias = bias_global))
}
res_gg <- future_sapply(theta, function(theta) {
sim_gg(B, n, mu, sigma, Q, theta)
})
# Exponencial
load("01_data//res_exp_n10_B1000_ncens100.RData")
plot_exp <- ggplot(res_exp) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
library(tidyverse)
# Exponencial
load("01_data//res_exp_n10_B1000_ncens100.RData")
plot_exp <- ggplot(res_exp) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_exp)
load("01_data//res_exp_n100_B1000_ncens100.RData")
plot_exp <- ggplot(res_exp) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_exp)
load("01_data//res_exp_n1000_B1000_ncens100.RData")
plot_exp <- ggplot(res_exp) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_exp)
load("01_data//res_wb_n1000_B1000_ncens100.RData")
plot_wb <- ggplot(res_wb) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_wb)
load("01_data//res_wb_n100_B1000_ncens100.RData")
plot_wb <- ggplot(res_wb) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_wb)
load("01_data//res_ln_n1000_B1000_ncens100.RData")
plot_ln <- ggplot(res_ln) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_ln)
load("01_data//res_ln_n100_B1000_ncens100.RData")
plot_ln <- ggplot(res_ln) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_ln)
load("01_data//res_ln_n100_B1000_ncens100.RData")
# ESTUDO APLICADO DO MÉTODO DE SIMULAÇÃO DE MONTE CARLO
# ________________________________________________________
# AVALIANDO O VIÉS DE ESTIMADORES DA FUNÇÃO DE SOBREVIVÊNCIA
# EM AMOSTRAS DE DIFERENTES PERCENTUAIS DE CENSURA:
# UMA APLICAÇÃO DO MÉTODO DE MONTE CARLO
# ________________________________________________________
# ================ 0. PACOTES NECESSÁRIOS ================
# ________________________________________________________
# Carregando pacotes...
library(tidyverse)
library(survival)
library(flexsurv)
library(future)
library(future.apply)
plan(multisession, workers = parallel::detectCores() - 1)
# ______________________________________________________________
# =========== 1. DEFINIÇÃO DE PARÂMETROS DA SIMULAÇÃO ==========
# ______________________________________________________________
# Tamanho amostral
n <- 1e2
# Número de simulações
B <- 1e1
# Número de percentuais de censura
n_cens_perc <- 1e2
# Tempos até o evento ~ Distribuição Exponencial
lambda <- 0.3
# # Tempos de censura ~ Distribuição Uniforme Contínua
calc_cens <- function(theta, n = 20000){
T_ <- rexp(n, rate = 1/lambda)
C_ <- runif(n, 0, theta)
mean(C_ < T_)
}
theta_grid <- exp(seq(log(0.001), log(1e3), length.out = n_cens_perc))
cens_grid <- sapply(theta_grid, calc_cens)
df_calib <- data.frame(
cens = cens_grid,
theta = theta_grid
) |>
arrange(cens) |>
group_by(cens) |>
summarise(theta = mean(theta), .groups = "drop")
theta_from_cens <- function(target_cens){
approx(
x = df_calib$cens,
y = df_calib$theta,
xout = target_cens,
rule = 2
)$y
}
cens_targets <- seq(0.05, 0.95, length.out = n_cens_perc)
theta <- sapply(cens_targets, theta_from_cens)
# ______________________________________________________________
# =============== 2. GERANDO AMOSTRAS ALEATÓRIAS ===============
# ______________________________________________________________
# Função para gerar amostras aleatórias de dados de sobrevivência com censura
# para a distribuição Gamma Generalizada
aa.exp <- function(n, lambda, theta) {
# Tempos até o evento
T_ <- rexp(n = n, rate = 1/lambda)
# Tempos de censura
C_ <- runif(n, min = 0, max = theta)
# Tempos Observados
tempos <- pmin(T_, C_)
# Indicador de censura
cens <- as.integer(T_ <= C_)
# Tempos observados com indicador de censura
X <- cbind(tempos, cens) |> as.data.frame()
colnames(X) <- c("time", "event")
return(X)
}
# ________________________________________________________
# ================= 3. ESTIMAÇÃO DE S(t) =================
# ________________________________________________________
fit.exp <- function(n = n, lambda = lambda, theta = theta){
# Gera amostra aleatória
X <- aa.exp(n = n, lambda = lambda, theta = theta)
# Modelo Paraamétrico: Distribuição Gamma Generalizada
# Ajusta parâmetros usando MLE
fit <- flexsurvreg(
Surv(time, event) ~ 1
,data = X
,dist = "exponential"
)
# Extraindo estimativas pontuais
lambda_hat <- 1/exp(fit$coefficients[1])
# Percentual de censura
cens_perc <- mean(1 - X$event)
# Retorna as estimativas pontuais
return(c(c = cens_perc, lambda = lambda_hat))
}
# ________________________________________________________
# ================= 3. CALCULANDO O VIÉS =================
# ________________________________________________________
# Definindo um grid de valores de t
t_exp <- seq(0, quantile(rexp(1e5, rate = 1/lambda), 0.99), length.out = 1e4)
# Função de sobrevivência teórica
S_t_teorica_exp <- pexp(q = t_exp, rate = 1/lambda, lower.tail = FALSE)
# Função de sobrevivência estimada
S_t_estimada_exp <- function(t, lambda){
pexp(q = t, rate = 1/lambda, lower.tail = FALSE)
}
# Calcula viés para cada percentual de censura
sim_exp <- function(B, n, lambda, theta){
# Gerando amostras e estimando
params_ams_exp <- replicate(
B,
fit.exp(
n = n,
lambda = lambda,
theta = theta
)
) |> t()
# Estimando S(t) para cada simulação
S_t_estimado_ams_exp <- sapply(
params_ams_exp[, "lambda"]
,function(lambda) S_t_estimada_exp(t_exp, lambda)
)
S_t_estimado_ams_mean_exp <- rowMeans(S_t_estimado_ams_exp)
# Estimando o viés
cens_perc <- mean(params_ams_exp[, "c"])
bias_S_t <- mean(S_t_estimado_ams_mean_exp/S_t_teorica_exp - 1)
bias_lambda <- mean(params_ams_exp[, "lambda"]/lambda - 1)
return(c(c = cens_perc, bias_S_t = bias_S_t, bias_lambda = bias_lambda))
}
# res_exp <- sapply(theta, function(x) sim_exp(B, n, lambda, theta = x))
res_exp <- future_sapply(
theta,
function(x) {
sim_exp(
B = B,
n = n,
lambda = lambda,
theta = x
)
}
)
res_exp <- as.data.frame(t(res_exp))
path <- paste0("01_data//res_exp_", "n", n, "_B", B, "_ncens", n_cens_perc, ".RData")
save(res_exp, file = path)
plot_exp <- ggplot(res_exp) +
aes(x = c, y = bias) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_exp)
res_exp
esquisse:esquisser()
esquisse::esquisser()
dados <- rbind(
data.frame(c = res_exp$c, bias_S_t = res_exp$bias_S_t, bias_lambda = res_exp$bias_lambda, tipo = "S(t)"),
data.frame(c = res_exp$c, bias_S_t = res_exp$bias_lambda, bias_lambda = res_exp$bias_lambda, tipo = "lambda")
)
dados
dados <- rbind(
data.frame(c = res_exp$c, bias = res_exp$bias_S_t, bias_lambda = res_exp$bias_lambda, tipo = "S(t)"),
data.frame(c = res_exp$c, bias = res_exp$bias_lambda, bias_lambda = res_exp$bias_lambda, tipo = "lambda")
) %>% select(-bias_lambda)
dados
esquisse::esquisser()
dados <- rbind(
data.frame(c = res_exp$c, bias = res_exp$bias_S_t, bias_lambda = res_exp$bias_lambda, tipo = "S(t)"),
data.frame(c = res_exp$c, bias = res_exp$bias_lambda, bias_lambda = res_exp$bias_lambda, tipo = "lambda")
) %>% select(-bias_lambda)
plot_exp <- ggplot(res_exp) +
aes(x = c, y = bias_S_t) +
geom_line(colour = "#112446") +
theme_minimal()
plotly::ggplotly(plot_exp)
plot_bias <- ggplot(dados) +
aes(x = c, y = bias, colour = tipo) +
geom_line() +
scale_color_hue(direction = 1) +
theme_minimal()
plotly::ggplotly(plot_bias)
# ESTUDO APLICADO DO MÉTODO DE SIMULAÇÃO DE MONTE CARLO
# ________________________________________________________
# AVALIANDO O VIÉS DE ESTIMADORES DA FUNÇÃO DE SOBREVIVÊNCIA
# EM AMOSTRAS DE DIFERENTES PERCENTUAIS DE CENSURA:
# UMA APLICAÇÃO DO MÉTODO DE MONTE CARLO
# ________________________________________________________
# ================ 0. PACOTES NECESSÁRIOS ================
# ________________________________________________________
# Carregando pacotes...
library(tidyverse)
library(survival)
library(flexsurv)
library(future)
library(future.apply)
plan(multisession, workers = parallel::detectCores() - 1)
# ______________________________________________________________
# =========== 1. DEFINIÇÃO DE PARÂMETROS DA SIMULAÇÃO ==========
# ______________________________________________________________
# Tamanho amostral
n <- 1e3
# Número de simulações
B <- 1e2
# Número de percentuais de censura
n_cens_perc <- 1e2
# Tempos até o evento ~ Distribuição Exponencial
lambda <- 0.3
# # Tempos de censura ~ Distribuição Uniforme Contínua
calc_cens <- function(theta, n = 20000){
T_ <- rexp(n, rate = 1/lambda)
C_ <- runif(n, 0, theta)
mean(C_ < T_)
}
theta_grid <- exp(seq(log(0.001), log(1e3), length.out = n_cens_perc))
cens_grid <- sapply(theta_grid, calc_cens)
df_calib <- data.frame(
cens = cens_grid,
theta = theta_grid
) |>
arrange(cens) |>
group_by(cens) |>
summarise(theta = mean(theta), .groups = "drop")
theta_from_cens <- function(target_cens){
approx(
x = df_calib$cens,
y = df_calib$theta,
xout = target_cens,
rule = 2
)$y
}
cens_targets <- seq(0.05, 0.95, length.out = n_cens_perc)
theta <- sapply(cens_targets, theta_from_cens)
# ______________________________________________________________
# =============== 2. GERANDO AMOSTRAS ALEATÓRIAS ===============
# ______________________________________________________________
# Função para gerar amostras aleatórias de dados de sobrevivência com censura
# para a distribuição Gamma Generalizada
aa.exp <- function(n, lambda, theta) {
# Tempos até o evento
T_ <- rexp(n = n, rate = 1/lambda)
# Tempos de censura
C_ <- runif(n, min = 0, max = theta)
# Tempos Observados
tempos <- pmin(T_, C_)
# Indicador de censura
cens <- as.integer(T_ <= C_)
# Tempos observados com indicador de censura
X <- cbind(tempos, cens) |> as.data.frame()
colnames(X) <- c("time", "event")
return(X)
}
# ________________________________________________________
# ================= 3. ESTIMAÇÃO DE S(t) =================
# ________________________________________________________
fit.exp <- function(n = n, lambda = lambda, theta = theta){
# Gera amostra aleatória
X <- aa.exp(n = n, lambda = lambda, theta = theta)
# Modelo Paraamétrico: Distribuição Gamma Generalizada
# Ajusta parâmetros usando MLE
fit <- flexsurvreg(
Surv(time, event) ~ 1
,data = X
,dist = "exponential"
)
# Extraindo estimativas pontuais
lambda_hat <- 1/exp(fit$coefficients[1])
# Percentual de censura
cens_perc <- mean(1 - X$event)
# Retorna as estimativas pontuais
return(c(c = cens_perc, lambda = lambda_hat))
}
# ________________________________________________________
# ================= 3. CALCULANDO O VIÉS =================
# ________________________________________________________
# Definindo um grid de valores de t
t_exp <- seq(0, quantile(rexp(1e5, rate = 1/lambda), 0.99), length.out = 1e4)
# Função de sobrevivência teórica
S_t_teorica_exp <- pexp(q = t_exp, rate = 1/lambda, lower.tail = FALSE)
# Função de sobrevivência estimada
S_t_estimada_exp <- function(t, lambda){
pexp(q = t, rate = 1/lambda, lower.tail = FALSE)
}
# Calcula viés para cada percentual de censura
sim_exp <- function(B, n, lambda, theta){
# Gerando amostras e estimando
params_ams_exp <- replicate(
B,
fit.exp(
n = n,
lambda = lambda,
theta = theta
)
) |> t()
# Estimando S(t) para cada simulação
S_t_estimado_ams_exp <- sapply(
params_ams_exp[, "lambda"]
,function(lambda) S_t_estimada_exp(t_exp, lambda)
)
S_t_estimado_ams_mean_exp <- rowMeans(S_t_estimado_ams_exp)
# Estimando o viés
cens_perc <- mean(params_ams_exp[, "c"])
bias_S_t <- mean(S_t_estimado_ams_mean_exp/S_t_teorica_exp - 1)
bias_lambda <- mean(params_ams_exp[, "lambda"]/lambda - 1)
return(c(c = cens_perc, bias_S_t = bias_S_t, bias_lambda = bias_lambda))
}
# res_exp <- sapply(theta, function(x) sim_exp(B, n, lambda, theta = x))
res_exp <- future_sapply(
theta,
function(x) {
sim_exp(
B = B,
n = n,
lambda = lambda,
theta = x
)
}
)
# ESTUDO APLICADO DO MÉTODO DE SIMULAÇÃO DE MONTE CARLO
# ________________________________________________________
# AVALIANDO O VIÉS DE ESTIMADORES DA FUNÇÃO DE SOBREVIVÊNCIA
# EM AMOSTRAS DE DIFERENTES PERCENTUAIS DE CENSURA:
# UMA APLICAÇÃO DO MÉTODO DE MONTE CARLO
# ________________________________________________________
# ================ 0. PACOTES NECESSÁRIOS ================
# ________________________________________________________
# Carregando pacotes...
library(tidyverse)
library(survival)
library(flexsurv)
library(future)
library(future.apply)
plan(multisession, workers = parallel::detectCores() - 1)
# ______________________________________________________________
# =========== 1. DEFINIÇÃO DE PARÂMETROS DA SIMULAÇÃO ==========
# ______________________________________________________________
# Tamanho amostral
n <- 1e3
# Número de simulações
B <- 1e2
# Número de percentuais de censura
n_cens_perc <- 1e2
# Tempos até o evento ~ Distribuição Exponencial
lambda <- 0.3
# # Tempos de censura ~ Distribuição Uniforme Contínua
calc_cens <- function(theta, n = 20000){
T_ <- rexp(n, rate = 1/lambda)
C_ <- runif(n, 0, theta)
mean(C_ < T_)
}
theta_grid <- exp(seq(log(0.001), log(1e3), length.out = n_cens_perc))
cens_grid <- sapply(theta_grid, calc_cens)
df_calib <- data.frame(
cens = cens_grid,
theta = theta_grid
) |>
arrange(cens) |>
group_by(cens) |>
summarise(theta = mean(theta), .groups = "drop")
theta_from_cens <- function(target_cens){
approx(
x = df_calib$cens,
y = df_calib$theta,
xout = target_cens,
rule = 2
)$y
}
cens_targets <- seq(0.05, 0.95, length.out = n_cens_perc)
theta <- sapply(cens_targets, theta_from_cens)
# ______________________________________________________________
# =============== 2. GERANDO AMOSTRAS ALEATÓRIAS ===============
# ______________________________________________________________
# Função para gerar amostras aleatórias de dados de sobrevivência com censura
# para a distribuição Gamma Generalizada
aa.exp <- function(n, lambda, theta) {
# Tempos até o evento
T_ <- rexp(n = n, rate = 1/lambda)
# Tempos de censura
C_ <- runif(n, min = 0, max = theta)
# Tempos Observados
tempos <- pmin(T_, C_)
# Indicador de censura
cens <- as.integer(T_ <= C_)
# Tempos observados com indicador de censura
X <- cbind(tempos, cens) |> as.data.frame()
colnames(X) <- c("time", "event")
return(X)
}
# ________________________________________________________
# ================= 3. ESTIMAÇÃO DE S(t) =================
# ________________________________________________________
fit.exp <- function(n = n, lambda = lambda, theta = theta){
# Gera amostra aleatória
X <- aa.exp(n = n, lambda = lambda, theta = theta)
# Modelo Paraamétrico: Distribuição Gamma Generalizada
# Ajusta parâmetros usando MLE
fit <- flexsurvreg(
Surv(time, event) ~ 1
,data = X
,dist = "exponential"
)
# Extraindo estimativas pontuais
lambda_hat <- 1/exp(fit$coefficients[1])
# Percentual de censura
cens_perc <- mean(1 - X$event)
# Retorna as estimativas pontuais
return(c(c = cens_perc, lambda = lambda_hat))
}
# ________________________________________________________
# ================= 3. CALCULANDO O VIÉS =================
# ________________________________________________________
# Definindo um grid de valores de t
t_exp <- seq(0, quantile(rexp(1e5, rate = 1/lambda), 0.99), length.out = 1e4)
# Função de sobrevivência teórica
S_t_teorica_exp <- pexp(q = t_exp, rate = 1/lambda, lower.tail = FALSE)
# Função de sobrevivência estimada
S_t_estimada_exp <- function(t, lambda){
pexp(q = t, rate = 1/lambda, lower.tail = FALSE)
}
# Calcula viés para cada percentual de censura
sim_exp <- function(B, n, lambda, theta){
# Gerando amostras e estimando
params_ams_exp <- replicate(
B,
fit.exp(
n = n,
lambda = lambda,
theta = theta
)
) |> t()
# Estimando S(t) para cada simulação
S_t_estimado_ams_exp <- sapply(
params_ams_exp[, "lambda"]
,function(lambda) S_t_estimada_exp(t_exp, lambda)
)
S_t_estimado_ams_mean_exp <- rowMeans(S_t_estimado_ams_exp)
# Estimando o viés
cens_perc <- mean(params_ams_exp[, "c"])
bias_S_t <- mean(S_t_estimado_ams_mean_exp/S_t_teorica_exp - 1)
bias_lambda <- mean(params_ams_exp[, "lambda"]/lambda - 1)
return(c(c = cens_perc, bias_S_t = bias_S_t, bias_lambda = bias_lambda))
}
# res_exp <- sapply(theta, function(x) sim_exp(B, n, lambda, theta = x))
res_exp <- future_sapply(
theta,
function(x) {
sim_exp(
B = B,
n = n,
lambda = lambda,
theta = x
)
}
)
